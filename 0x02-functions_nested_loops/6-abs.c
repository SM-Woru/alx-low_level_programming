#include "main.h"

/**
 * _abs - a function computes the absolute value of an integer.
 *
 * Return: The absolute value of the integer.
 */

int _abs(int i)
{
  if (i >= 0)
    return (i);
  else
    return (-i);
}
